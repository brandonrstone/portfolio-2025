export const AnimatedGradientBackground = ({ children }: { children?: React.ReactNode }) => (
  <section className="animated-gradient w-full h-full min-h-screen flex items-center justify-center">
    {children}
  </section>
)